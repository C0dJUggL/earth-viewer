# Earth viewer
In order for the program to work, you must add your config to `config.py`.  This can be done if you register at `https://api.nasa.gov/` and get your *API key* .  it should be entered into the
```python
 nasa_key=" "
```
variable.  After that everything should work :)
